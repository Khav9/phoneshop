spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/phoneshop_night
    username: khavdb
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update   # use: none | validate | update | create | create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect